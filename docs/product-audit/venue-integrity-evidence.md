# Venue integrity implementation evidence

Working-tree implementation, September 8, 2026. These changes are not deployed. Baseline audit remains `42b567c`; this document records pending fixes rather than attributing them to the live app.

## Requirement coverage

| Requirement | Implemented behavior | Verification |
| --- | --- | --- |
| CV-02 | Reviewed, published venue structured hours override community hours in map cards, Open now filtering, court detail and owner court-context preview. Unpublish/release restores untouched community data. Weekly split periods, explicit closed versus unknown days, venue timezone and dated holiday exceptions share one evaluator; holidays cut off overnight carry-in. Owner editor supports weekday copying and a single save for hours plus visiting notes. | `test_court_venue_hours.py`; combined206 checks passed, final UI checks run separately. Browser saved and reopened Monday–Friday08:00–20:00 plus Christmasclosure at an actual390×844 iframe viewport with scrollWidth390; `mobile-weekly-hours.png`, `weekly-holiday-editor.png`. |
| CV-17 | Missing location stays inside claiming: address lookup and chosen coordinates, duplicate choices, role step, then one atomic location/claim submission. Pending location is private, editable by its owner, and explicitly reviewed before court publication. Ordinary community Add court remains immediate. | `test_business_missing_locations.py` covers private discovery/detail, player-action exclusion, duplicate privacy, rollback without orphan rows, If-Match correction, reviewer MFA/evidence/location gates, separate business publication. Browser submitted synthetic Demo Court Club on isolated8053 and reopened its private workspace. |
| CV-18 | Submission time, concrete evidence/review next step, support link, correct-location action, and separate management/content/publication progress. No fabricated review deadline. | Browser `missing-location-submitted.png` shows Under review / Prepare your details / Private and correct address. |
| CV-19 | Last reviewed content remains public while edits await review. Safe approved links/logo remain intact; known unsafe destinations are held individually. Explicit publish after approval. | `test_business_reviewed_versions.py` covers court/detail/booking action projection, pending collections, unsafe URL scope and logo bytes. Compact discovery batches related content and safety checks; the query-count regression passes. |
| CV-20 | Text forms keep unsaved state; logo operations have their own busy/saved/error/retry status and history/undo entry. Collection editing stages changes until Save; direct session Save is explicit. Successful child saves retain unrelated text drafts. | Frontend behavior tests exercise success/failure, changed fields, logo response rebasing and discard. Persistent workspace preview marks saved private/draft/live state. |
| CV-21 | Full court page preview uses only the manager's proposed venue content in an isolated slot; preview label remains visible and booking/player actions are disabled. | `full-court-preview.png`; reviewed-version and frontend tests. Preview does not mutate shared court cache or count booking activity. |
| CV-22 | Dated agenda, Today and previous/next week, repeating-pattern view, duplicate and cancel date. Exact-date and following-date overrides preserve other weeks and historical dates. | `test_business_dated_agenda.py`, `mobile-agenda.png`, `mobile-cancelled-date.png`: cancelled Sep12 remains cancelled; Sep19 is scheduled. Earlier browser proof measured actual390px document width. |
| CV-23 | Venue timezone is stored and inherited from explicit venue/court/session context. Human-readable selector; no automatic manager-device timezone default. | DST and inherited timezone tests; per-date wall-clock agenda checks. |
| CV-24 | Spots/capacity, booking destination and checked time are visible. Manual availability older than two days or unknown becomes Check availability. Editing a title does not refresh inventory; explicit recheck can apply only to one date. | Dated agenda tests cover stale inventory, scoped recheck and spoofed timestamp rejection. Restore retains historical checked time. |
| CV-25 | Services tab and templates for private lesson, group clinic, court rental and membership. Public service has a direct booking destination or an explicit venue inquiry contact; no fabricated booking action when neither exists. | Frontend renderer/safe-link tests; reviewed contact projection. No native payment or real-time inventory claim. |
| CV-26 | External booking destination hostname and new-site behavior are visible; specific dated session URL is retained. Original court context stays open. Booking click records activity only. | Manual/date link attribution tests validate server-owned subject names and reviewed URLs; frontend safe destination preview tests. |
| CV-27 | Main connection actions are Add booking link, Import schedule spreadsheet, and Get setup help. Raw provider contract/capabilities remain in Advanced; player-visible/import status and last update remain explicit. | Business hub/platform tests; existing spreadsheet staging remains the import path. Unsupported provider partnerships are not implied. |
| CV-28 | Three primary activity counts, chosen time interval, prior-period comparison, top clicked dated sessions. Unavailable conversions stay unavailable; no inferred conversion rate. Latest-request guard prevents old ranges overwriting new results. | New `test_business_activity_tasks.py`: correct previous/current intervals, future exclusion, legacy unknown names, validated subjects, event deduplication. Frontend delayed-response behavior test; browser `owner-analytics.png` and range interaction. |
| CV-29 | Field-level before/after history and restore preview, including readable per-date overrides. Restore creates a new version and preserves the reviewed public listing. | Reviewed-version restore tests and existing revision-diff frontend tests. |
| CV-30 | Required If-Match on content/logo/collection/restore writes; missing428/stale412. Three-way reconciliation preserves unrelated edits and exposes Latest/My conflict choices. No legacy same-path bypass. | Two-manager integration test, frontend collection recovery tests, `two-editor-conflict.png`. |
| CV-31 | Plain role capabilities, invitation expiry/renewal status, safe resend that rotates pending links and preserves the last valid link if email delivery fails. Operator search, type, assignment, overdue and ordering filters apply before row limits; stale result guards. | New activity tests cover expired/resend/token rotation/delivery rollback and operator scope/search/assignment/overdue. `team-capabilities.png`. Operator filter UI was not manually exercised with a privileged browser session; API and frontend contracts were checked. |

## Validation

All tests use explicit `APP_ENV=testing TEST_DATABASE_URL=sqlite:///:memory: DATABASE_URL=sqlite:///:memory: SCHEMA_MANAGEMENT_ENABLED=true AUTO_CREATE_DB=true`; no production DB calls. The combined venue/business suite plus relevant backend regressions passed189 tests with3 unrelated cases deselected. Final frontend copy checks ran separately. Node syntax checks for both venue JS files and `git diff --check` passed.

Screenshots are under `output/product-audit/venue-verification/`. The isolated browser fixture uses synthetic accounts, a cached synthetic address, and no live booking/provider writes. New top-session labels are validated on the server, not accepted from client prose. Historical unattributed events remain unknown. Schema migration/verification wiring is owned by the root integration task.

Remaining work: integrated release/bundle verification and deployment, optional privileged operator browser walk-through, and the remaining public court discovery, timeline, presence and contribution improvements. The legacy free-text hours field is retained as visiting information; it never supplies an inferred Open now value.

## Release closure: court timeline and venue entry — September 8, 2026

This section supersedes the earlier remaining-work sentence for the specific court timeline, selection, search-scope and entry-handoff items below. Source changes are frozen for root integration; this note does not claim deployment.

- **CV-01 / CV-05 / CV-07:** Selected and saved courts load their next dated opportunity from the same court timeline. The full court starts with Play here; player-organized sessions, venue dates and community-listed open play have separate source labels and actions. A player session offers View & join/Open session, an official date opens its validated external destination, and a community-listed time offers Plan to go without implying registration. The mixed timeline retains its week and refreshes after a child session closes.
- **CV-02:** Timeline intervals now use the public reviewed-hours projection, including exact-date holiday closures, split periods and overnight carry-in. Known conflicts show a source-specific warning. Community Plan to go is unavailable for a closed interval; an existing player session remains viewable and an external venue item says Confirm with venue. A pending owner draft that reopens the holiday cannot override the reviewed closure. Unknown hours and dawn-to-dusk hours do not fabricate an exact-time conflict.
- **CV-03:** Camera movement preserves typed search until the explicit Search this map area action. That action clears the query and loads the viewport; home-area selection stays separate. The native browser check used map zoom movement; drag panning was not separately exercised in this release check.
- **CV-07 / CV-26:** Plan to go re-reads the exact date before opening the planner and uses the fresh listed start, duration and notes. Venue entry, registration/reservation separation and supplied fee/access information are persisted as ordinary Game.description text. Both the signed-in session and public entry page surface that first paragraph before the roster/join action; ordinary host notes stay separately visible. It grants no admission, booking, visibility or membership rights.
- **Mobile release defect:** A long hours warning pushed selected-court actions below the peek dock. The portrait layout now keeps selected court identity and the details/directions footer fixed while the opportunity body scrolls. The full-details button was then clicked successfully at a real 390 × 844 viewport.

### Meaningful verification

The final targeted command passed **66 tests** across `test_business_hub_frontend.py`, `test_court_play_timeline.py`, `test_court_venue_hours.py`, `test_court_product_strategy_completion_frontend.py`, `test_public_entry_onboarding_frontend.py` and `test_game_planning_fields_frontend.py`. All used the explicit isolated in-memory environment described above. This includes actual Game creation using the frontend-generated description, retrieval by another player, and public share-preview retrieval; holiday conflict tests retain existing sessions and protect reviewed-versus-draft hours. The stale `cd-sec-games` source assertion now checks the intended `cd-play-here` → verified business → checked-in players hierarchy. `node --check public/app-v15.js` passed. The final portrait CSS adjustment was verified in the browser after that suite; root runs integrated frontend/release checks.

Native isolated Chrome session `venue-release` against synthetic in-memory fixture `8057` verified:

1. Normal sign-in as the synthetic Jordan account, search for Tustin Pickleball, selected verified venue and visible hours-conflict warning.
2. Map camera movement kept the query; Search this map area reset it explicitly and retained the separate home area.
3. Court details showed the mixed Thursday holiday closure, Friday community open play, Saturday official registration and player-organized session.
4. Friday Plan to go → exact-date check → two-hour planner → Schedule play session created Open play together. Venue-entry requirements appeared above Players.
5. Back through the retained handoff returned to the same court/week with the new player session immediately visible. `documentElement.clientWidth` and `scrollWidth` were both 390; the browser errors command returned no errors. No external booking destination was visited.

Screenshots in `output/product-audit/venue-verification/`: `release-map-selected-fixed.png`, `release-court-timeline.png`, `release-court-timeline-mixed.png`, `release-created-entry-note.png`, `release-court-return-refreshed.png`. `release-map-selected.png` intentionally records the pre-fix clipped footer and should not be used as the final UI screenshot.

Release-slice source: `backend/services/court_hours.py:152`, `backend/services/court_play.py:133`, `public/app-v15.js` helpers `courtTimelineItemHtml`, `courtOpenPlayPlanDescription`, `courtEntryDescriptionParts`, `courtEntryNoticeHtml`, `openCourtWindowPlan`, and the two approved calls in `publicShareDetailHtml` / `gameScreenHtml`; final portrait rules at `public/styles-v15.css:7389`. No schema or migration additions in this slice.

### Final frontend contract reconciliation

The release sweep exposed historical source assertions for removed Play options controls, day tabs, the former games section, and the old flat saved-court card. These now assert the dated timeline/arrival hierarchy, next-opportunity slot, native visibility radios and sibling court/session controls. The mobile transition harness includes the real presence-summary helper and records each next-opportunity load; the claim harness includes the pending-location step dependencies. No product source changed during this reconciliation. The five files `test_court_discovery_frontend.py`, `test_focused_workspaces_frontend.py`, `test_mobile_court_browsing_frontend.py`, `test_ui_interaction_foundation_frontend.py` and `test_play_game_ui_consistency_frontend.py` passed **118 tests in 2.99 seconds**. Their diff whitespace check passed. Full log: `tmp/product-audit-release/court-contracts.log`.
